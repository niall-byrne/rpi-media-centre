#!/bin/bash

# stdlib io library

set -eo pipefail

# shellcheck source=stdlib/io/filesystem/__lib__.sh
source "${STDLIB_DIRECTORY}/io/filesystem/__lib__.sh"
# shellcheck source=stdlib/io/stdin.sh
source "${STDLIB_DIRECTORY}/io/stdin.sh"
